apiVersion: apps/v1
kind: Deployment
metadata:
  name: argo-server
spec:
  template:
    spec:
      containers:
      - args:
        - server
        - --namespaced
        - --auth-mode
        - server
        name: argo-server
